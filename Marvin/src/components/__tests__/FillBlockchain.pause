import React from 'react';
import { mount } from 'enzyme'
import chai from 'chai'
import FillBlockchain from '../App/FillBlockchain';


describe('<FillBlockhain/>', () => {
    it('renders without exploding', () => {
        const wrapper = mount(<FillBlockchain />);
        chai.expect(wrapper.find(FillBlockchain).length).to.equal(1);
    });
});